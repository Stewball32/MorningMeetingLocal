// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
	darkMode: false,
	content: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		extend: {},
	},
	plugins: [],
}

export default config
